{
  "name": "kmartinezmedia",
  "version": "1.0.0",
  "description": "katherine martinez portfolio website",
  "private": true,
  "scripts": {
    "api": "npm run build && NODE_ENV=development nodemon server.js",
    "build": "nwb build-react-app --copy-files",
    "clean": "nwb clean-app",
    "staging": "npm update cmx-components && NODE_ENV=staging npm run build && NODE_ENV=staging pm2 restart celmatix-staging",
    "staging:local": "npm update cmx-components && NODE_ENV=staging npm run build && NODE_ENV=staging node server.js",
    "production": "npm update cmx-components && NODE_ENV=production npm run build && NODE_ENV=production pm2 restart celmatix-production",
    "production:local": "npm update cmx-components && NODE_ENV=production npm run build && NODE_ENV=production node server.js",
    "start": "NODE_ENV=development node server.js",
    "start-old": "nwb serve-react-app --port 8000",
    "setup": "node setup.js",
    "test": "nwb test-react",
    "test:coverage": "nwb test-react --coverage",
    "test:watch": "nwb test-react --server"
  },
  "dependencies": {
    "axios": "^0.15.3",
    "cmx-components": "^2.0.1",
    "object-assign": "^4.1.1",
    "react": "15.4.2",
    "react-dom": "15.4.2",
    "react-helmet": "^5.0.3",
    "react-router": "^2.8.0"
  },
  "devDependencies": {
    "airtable": "^0.4.5",
    "basic-auth": "^1.1.0",
    "body-parser": "^1.17.1",
    "dotenv": "^4.0.0",
    "express": "^4.15.2",
    "fs": "0.0.1-security",
    "nodemon": "^1.11.0",
    "nwb": "0.15.x",
    "nwb-sass": "^0.7.1"
  },
  "author": "Katherine Martinez",
  "license": "MIT",
  "repository": ""
}
